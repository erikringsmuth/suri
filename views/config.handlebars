// Copyright (C) 2014 Erik Ringsmuth - MIT license
define([], function() {
  return {
    session: {
      signedIn: {{#if signedIn}}true{{else}}false{{/if}},
      userId: '{{userId}}',
      displayName: '{{displayName}}',
      emailMd5: '{{emailMd5}}',
      authenticationMessage: '{{authenticationMessage}}'
    }
  };
});
